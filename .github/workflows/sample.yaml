name: Continuous Integration and Continuous Deployment

on:
  push:
    branches:
      - main

jobs:
  sample-dotnet-workflow:
  runs-on: sample
  steps:
    - name: checkout
      uses: actions/checkout@v3