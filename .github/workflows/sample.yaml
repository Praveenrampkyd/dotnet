name: Continuous Integration and Continuous Deployment

on:
  push:
    branches:
      - main

jobs:
  sample-dotnet-workflow:
  runs-on: self-hosted
  steps:
    - name: checkout
      uses: actions/checkout@v3